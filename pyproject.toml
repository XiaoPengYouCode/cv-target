[project]
name = "target-detector"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "opencv-python",
    "numpy",
    "kornia",
    "torch",
    "torchvision",
    "logging>=0.4.9.6",
]
